# RusoomPay — Arabic / English Language Layer

## الحالة
- العربية: اللغة الافتراضية والأساسية — جاهزة للإنتاج.
- الإنجليزية: خلف مفتاح `NEXT_PUBLIC_ENABLE_ENGLISH` (افتراضياً `false` — الزر لا يظهر ولا يعمل حتى تفعّله).
  تغطية القاموس الحالية ≈ 22% من نصوص الواجهة (299 من 1348 نصاً)؛ الباقي يبقى عربياً بدل أن يظهر مشوّهاً.
  فعّلها فقط بعد استكمال القاموس أو إن كنت راضياً بالتغطية الحالية لجمهور محدد.

## التفعيل
Netlify → Site settings → Environment variables:
`NEXT_PUBLIC_ENABLE_ENGLISH=true` ثم Trigger deploy (المتغيّر يُضمَّن وقت البناء، وليس وقت التشغيل).

## إصلاحات التدقيق — الجولة الأولى (25-09-2026)
1. حلقة لا نهائية في MutationObserver (~60 مسح كامل للصفحة/ثانية في الوضع الإنجليزي).
2. قيم قديمة (stale) بعد تحديث React لنص مترجم.
3. ترجمة جزئية مشوّهة («بDate»، «الAccountantي») وترجمة بيانات القاعدة.
4. الوضع العربي أصبح بلا Observer وبلا أي مسح للصفحة (صفر تكلفة أداء).
5. `public/sw.js` كان محفوظاً كـ PDF — أُعيد كملف JS.
6. middleware: `/about` أصبحت عامة؛ الملفات الثابتة لم تعد تُحوَّل لتسجيل الدخول.

## إصلاحات التدقيق — الجولة الثانية (تدقيق نهائي قبل الرفع)
7. **ترتيب كلمات خاطئ**: عبارات من عدة كلمات كانت تُترجم كلمة-كلمة فتُنتج ترتيباً إنجليزياً مكسوراً
   («name Employee required» بدل الإبقاء عربياً). الآن: تطابق قاموس كعبارة كاملة، أو تبقى عربية.
8. **جمل مركّبة عبر عدة عُقد نصية** (مثل `صفحة {page} من {total}` في JSX) كانت تُترجم عقدة بعقدة
   فتُنتج «Page 3 From 10». الآن تُعامل كل جملة كوحدة واحدة: إما تُترجم كلها أو تبقى عربية كلها.
9. **كلمات الربط القصيرة** («من»، «إلى»، «و»...) كانت تُترجم حتى عند ملاصقتها لقيمة رقمية
   («من 100» ← «From 100»، والصحيح «من أصل 100»). الآن تُترجم فقط إذا كانت العنوان الوحيد في العنصر.
10. **وميض عربي لحظي** قبل الترجمة عند إضافة محتوى جديد (نافذة منبثقة، سطر جديد في قائمة) —
    أُصلح بمعالجة المحتوى الجديد ضمن نفس دورة الـMutationObserver قبل أول رسم للصفحة.
11. **سهم اتجاه ثابت** (← في زر تسجيل الدخول) كان يشير للخلف في الوضع الإنجليزي — يُعكس تلقائياً الآن.
12. **شريط تمرير أفقي طفيف** (14-17px) في صفحتي `/subscribe` و`/privacy` بالوضع الإنجليزي،
    سببه علامات القوائم النقطية RTL؛ أُضيف `overflow-x: clip` لتناظر سلوك العربية.
13. **تعزيز المفتاح**: تبديل من تبويب آخر عبر `localStorage` لا يمكنه تفعيل الإنجليزية إن كان المفتاح معطّلاً.

## ملفات معدّلة
- `lib/i18n.ts` — محرّك الترجمة الآمن (مطابقة عبارات كاملة، وحدة قاموسية واحدة لكل نص، حراسة كلمات الربط) + مفتاح التفعيل.
- `components/i18n/LanguageProvider.tsx` — إعادة كتابة كاملة: معالجة على مستوى الجملة/المجموعة النصية، معالجة قبل الرسم، بلا حلقات.
- `components/i18n/LanguageSwitcher.tsx` — يختفي تماماً عند تعطيل الإنجليزية.
- `app/layout.tsx` — سكربت اللغة المبكر مشروط بالمفتاح (خط Cairo كما هو، بلا تعديل).
- `lib/invoice-pdf.ts`, `lib/print-report.ts` — يحترمان المفتاح.
- `middleware.ts`, `public/sw.js`, `.env.example`, `tests/unit/i18n.test.ts`, `app/globals.css`.

## معروف (قبل تفعيل الإنجليزية لاحقاً)
- ~79 عنصر بـ `direction: 'rtl'` مضمّن داخل الصفحات سيبقى RTL في الإنجليزية.
- النصوص غير الموجودة في القاموس تبقى عربية عمداً — الحل الدائم: مفاتيح ترجمة صريحة لكل نص بدل ترجمة الصفحة بعد عرضها.

## التحقق (شمل تصفّح فعلي حقيقي عبر Playwright/Chromium، وليس فقط بناء الكود)
- `npx tsc --noEmit` ✓
- `next build` ✓ — بالمفتاح مفعّلاً ومعطّلاً، 39 مساراً بلا أخطاء
- `vitest tests/unit` — 37/37 ✓ (يشمل 9 اختبارات مخصّصة لمحرك الترجمة)
- تصفّح حقيقي: عشرات سيناريوهات تبديل اللغة (أرقام تتغيّر، قوائم تُعاد ترتيبها، نوافذ منبثقة،
  حقول إدخال، select، 60 تبديلاً متتالياً مع تفاعل) على الجوال (390px) والكمبيوتر (1366px) — صفر أخطاء كونسول،
  صفر قيم قديمة، صفر حلقات، صفر تمرير أفقي، القائمة الجانبية تتبدّل صحيحاً في الاتجاهين.
